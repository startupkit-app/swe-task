{
  "request": {
    "method": "POST",
    "urlPathPattern": "/webhook-flaky/.*"
  },
  "response": {
    "status": 500,
    "headers": {
      "Content-Type": "application/json"
    },
    "jsonBody": {
      "error": "internal_server_error"
    },
    "fixedDelayMilliseconds": 50
  },
  "metadata": {
    "description": "Flaky webhook endpoint that always returns 500. Used for bonus webhook delivery testing — candidates must implement retry logic."
  }
}
